<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - EntreLinhas</title>
    <meta name="description" content="Faça login no EntreLinhas para acessar sua conta e publicar seus textos.">
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="../assets/images/jornal.png">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Source+Sans+Pro:wght@400;600;700&display=swap" rel="stylesheet">
    <!-- CSS -->
    <link rel="stylesheet" href="../assets/css/style.css">
    <link rel="stylesheet" href="../assets/css/alerts.css">
    <link rel="stylesheet" href="../assets/css/user-menu.css">
    <style>
        .error {
            border-color: var(--error) !important;
        }
        .form-container {
            margin-top: 2rem;
        }
        .alert {
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 4px;
        }
        .alert-success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        #debug-panel {
            display: none;
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background-color: rgba(0,0,0,0.9);
            color: #00ff00;
            font-family: monospace;
            padding: 10px;
            max-height: 300px;
            overflow-y: auto;
            z-index: 9999;
            font-size: 12px;
        }
        #debug-panel.visible {
            display: block;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <nav class="navbar">
            <div class="logo">
                <a href="index.html">EntreLinhas</a>
            </div>
            
            <ul class="nav-links">
                <li><a href="index.html">Início</a></li>
                <li><a href="artigos.html">Artigos</a></li>
                <li><a href="sobre.html">Sobre</a></li>
                <li><a href="escola.html">A Escola</a></li>
                <li><a href="contato.html">Contato</a></li>
            </ul>
            
            <div class="nav-buttons">
                <a href="login.html" class="btn btn-secondary active">Entrar</a>
                <button id="theme-toggle" class="theme-toggle" aria-label="Alternar modo escuro">
                    <i class="fas fa-moon"></i>
                </button>
                <button id="mobile-menu-btn" class="mobile-menu-btn" aria-label="Menu">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="container">
        <div class="form-container fade-in">
            <h1 class="form-title">Entrar no EntreLinhas</h1>
            
            <div id="mensagem-resultado" style="display: none;" class="alert"></div>
            
            <form id="login-form">
                <div class="form-group">
                    <label for="email">E-mail</label>
                    <input type="email" id="email" name="email" required>
                </div>
                
                <div class="form-group">
                    <label for="password">Senha</label>
                    <input type="password" id="password" name="senha" required>
                </div>
                
                <div class="form-group">
                    <label style="display: flex; align-items: center; gap: 0.5rem;">
                        <input type="checkbox" id="remember" name="remember">
                        <span>Lembrar de mim</span>
                    </label>
                </div>
                
                <button type="submit" id="login-btn" class="btn btn-primary w-full">Entrar</button>
            </form>
            
            <p class="text-center mt-3">
                <a href="esqueci-senha.html">Esqueceu a senha?</a>
            </p>
            
            <hr style="margin: 2rem 0; border: none; border-top: 1px solid var(--border-light);">
            
            <p class="text-center">
                Ainda não tem uma conta? <a href="cadastro.html">Cadastre-se</a>
            </p>

            <!-- Debug Controls -->
            <div style="margin-top: 30px; border-top: 1px solid #eee; padding-top: 10px;">
                <button onclick="mostrarPainel()" class="btn btn-secondary" style="font-size: 0.8rem;">Painel de Debug</button>
                <button onclick="limparLocalStorage()" class="btn btn-secondary" style="font-size: 0.8rem;">Limpar Login</button>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer>
        <div class="footer-container">
            <div class="footer-section">
                <h3>EntreLinhas</h3>
                <p>Um jornal digital colaborativo onde vozes diversas se encontram para compartilhar conhecimento, histórias e experiências.</p>
            </div>
            
            <div class="footer-section">
                <h3>Links Rápidos</h3>
                <ul class="footer-links">
                    <li><a href="index.html">Início</a></li>
                    <li><a href="artigos.html">Artigos</a></li>
                    <li><a href="sobre.html">Sobre</a></li>
                    <li><a href="escola.html">A Escola</a></li>
                    <li><a href="contato.html">Contato</a></li>
                </ul>
            </div>
            
            <div class="footer-section">
                <h3>Contato</h3>
                <ul class="footer-links">
                    <li><i class="fas fa-envelope"></i> jimmycastilho555@gmail.com</li>
                    <li><i class="fas fa-map-marker-alt"></i> Av. Marechal Rondon, 3000 - Jardim Bandeirantes, Salto - SP</li>
                    <li><i class="fas fa-phone"></i> (11) 4029-1234</li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2025 EntreLinhas - SESI Salto. Todos os direitos reservados.</p>
        </div>
    </footer>

    <!-- Debug Panel -->
    <div id="debug-panel"></div>

    <!-- JavaScript -->
    <script src="../assets/js/main.js"></script>
    <script src="../assets/js/debug.js"></script>
    <script src="../assets/js/header-nav.js"></script>
    
    <!-- Script para depuração e login -->
    <script>
    // Funções de depuração
    function mostrarPainel() {
        const panel = document.getElementById('debug-panel');
        panel.classList.toggle('visible');
        
        if (panel.classList.contains('visible')) {
            panel.innerHTML = '<strong>Painel de Debug:</strong><br>';
            
            // Exibir informações básicas
            panel.innerHTML += 'Login integrado com auth.js<br>';
            
            // Verificar localStorage
            try {
                let lsContent = 'localStorage: ';
                for (let i = 0; i < localStorage.length; i++) {
                    const key = localStorage.key(i);
                    lsContent += `${key}="${localStorage.getItem(key)}", `;
                }
                panel.innerHTML += lsContent + '<br>';
            } catch (err) {
                panel.innerHTML += 'Erro ao acessar localStorage: ' + err.message + '<br>';
            }
            
            // Verificar formulário
            const form = document.getElementById('login-form');
            panel.innerHTML += 'Formulário encontrado: ' + (form ? 'Sim' : 'Não') + '<br>';
            
            // Exibir usuários demo
            panel.innerHTML += 'Usuários demo disponíveis:<br>';
            panel.innerHTML += '- admin@exemplo.com / admin123<br>';
            panel.innerHTML += '- usuario@exemplo.com / usuario123<br>';
        }
    }
    
    function limparLocalStorage() {
        localStorage.removeItem('userLoggedIn');
        localStorage.removeItem('userName');
        localStorage.removeItem('userEmail');
        localStorage.removeItem('userType');
        localStorage.removeItem('userId');
        
        alert('Dados de login removidos com sucesso!');
        mostrarMensagem('Dados de login removidos com sucesso!', 'success');
    }
    
    function mostrarMensagem(texto, tipo) {
        const mensagemDiv = document.getElementById('mensagem-resultado');
        mensagemDiv.textContent = texto;
        mensagemDiv.className = `alert alert-${tipo}`;
        mensagemDiv.style.display = 'block';
        
        // Esconder após alguns segundos
        setTimeout(() => {
            mensagemDiv.style.display = 'none';
        }, 5000);
    }
    
    // Verificar se o usuário já está logado ao carregar a página
    document.addEventListener('DOMContentLoaded', () => {
        console.log('Página de login carregada');
        
        // Verificar se há parâmetro de forçar logout na URL
        const urlParams = new URLSearchParams(window.location.search);
        const forceLogout = urlParams.get('logout');
        
        if (forceLogout === 'true') {
            // Limpar dados de login
            limparLocalStorage();
            mostrarMensagem('Você foi desconectado com sucesso!', 'success');
        }
        
        // Verificar se o usuário já está logado
        const isLoggedIn = localStorage.getItem('userLoggedIn') === 'true';
        if (isLoggedIn && forceLogout !== 'true') {
            // Redirecionar para a página inicial
            mostrarMensagem('Você já está logado. Redirecionando...', 'success');
            setTimeout(() => {
                window.location.href = 'index.html';
            }, 1000);
        }
        
        // Adicionar evento ao formulário de login
        const loginForm = document.getElementById('login-form');
        if (loginForm) {
            loginForm.addEventListener('submit', function(e) {
                e.preventDefault();
                
                // Obter dados do formulário
                const email = document.getElementById('email').value.trim().toLowerCase();
                const password = document.getElementById('password').value;
                
                // Atualizar botão
                const loginBtn = document.getElementById('login-btn');
                const originalText = loginBtn.textContent;
                loginBtn.textContent = 'Processando...';
                loginBtn.disabled = true;
                
                // Obter usuários registrados (combinando demo + registrados)
                const demoUsers = [
                    { 
                        email: 'admin@exemplo.com', 
                        senha: 'admin123', 
                        nome: 'Administrador',
                        tipo: 'admin',
                        id: '1'
                    },
                    { 
                        email: 'usuario@exemplo.com', 
                        senha: 'usuario123', 
                        nome: 'Usuário Teste',
                        tipo: 'usuario',
                        id: '2'
                    }
                ];
                
                // Obter usuários registrados do localStorage
                const registeredUsers = JSON.parse(localStorage.getItem('users') || '[]');
                const allUsers = [...demoUsers, ...registeredUsers];
                
                // Verificar usuário
                const user = allUsers.find(u => u.email.toLowerCase() === email && u.senha === password);
                
                setTimeout(() => {
                    if (user) {
                        // Login bem-sucedido
                        mostrarMensagem('Login realizado com sucesso! Redirecionando...', 'success');
                        
                        // Salvar dados do usuário no localStorage
                        localStorage.setItem('userLoggedIn', 'true');
                        localStorage.setItem('userName', user.nome);
                        localStorage.setItem('userEmail', user.email);
                        localStorage.setItem('userType', user.tipo);
                        localStorage.setItem('userId', user.id);
                        
                        // Redirecionar após 1 segundo
                        setTimeout(() => {
                            window.location.href = 'index.html';
                        }, 1000);
                    } else {
                        // Login falhou
                        mostrarMensagem('Email ou senha incorretos. Por favor, tente novamente.', 'danger');
                        
                        // Restaurar botão
                        loginBtn.textContent = originalText;
                        loginBtn.disabled = false;
                    }
                }, 500);
            });
        } else {
            console.error('Formulário de login não encontrado!');
            alert('Erro: Formulário de login não encontrado. Por favor, recarregue a página.');
        }
    });
    </script>
</body>
</html>